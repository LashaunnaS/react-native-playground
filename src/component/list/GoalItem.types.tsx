import {ListRenderItemInfo} from 'react-native';
import {GoalItemProps} from '../root/AppRoot/AppRoot.types';

export interface GoalItemListitemProps {
  goalItem: ListRenderItemInfo<GoalItemProps>;
}
